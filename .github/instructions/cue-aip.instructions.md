---
applyTo: "**"
excludeAgent: "code-review"
---

Read `AGENTS.md`, `README.md`, and `docs/README.md` first.

If outbound HTTPS is allowed, optional intake is described in `agent-intake.json`.

Do not commit generated packs or logs under `outputs/aip_packs/` or `outputs/aip_server/`.
