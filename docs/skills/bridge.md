# Skill: Bridge

## What it does
Finds the aesthetic midpoint between two selected images.

## Requirements
- Exactly 2 selected images.

## How it works
- Sends `/bridge <a> <b>` over PTY.
- Engine returns a midpoint interpretation.

## Desired effect
A coherent in-between visual language (not a collage).
