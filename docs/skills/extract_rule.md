# Skill: Extract the Rule

## What it does
Infers the shared hidden rule across three selected images.

## Requirements
- Exactly 3 selected images.

## How it works
- Sends `/extract_rule <a> <b> <c>` over PTY.
- Engine returns principle + visual evidence payload.

## Desired effect
A reusable articulation of the style/system rule.
